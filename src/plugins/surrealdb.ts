/**
 * SurrealDB plugin (placeholder)
 *
 * TODO: Implement following TDD principles
 */

import type { DottedOptions } from '../types.js';

export interface SurrealDBPluginOptions {
  // TODO: Define options
}

export function withSurrealDB(_options: SurrealDBPluginOptions): Partial<DottedOptions> {
  throw new Error('Not yet implemented - following TDD, tests must be written first');
}
